version: 1
build:
  env:
    PYTHONUNBUFFERED: "1"
  dockerfile: Dockerfile

services:
  - type: web
    name: pyro4-server
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
